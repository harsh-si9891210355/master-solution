# StreamHandler service dependencies.
#
# GStreamer Python bindings (PyGObject / `gi`) are intentionally NOT installed
# via pip — they ship as system packages (python3-gi + gstreamer1.0-*) and are
# apt-installed in the Dockerfile. The GStreamer decoder import-guards on `gi`
# and the service falls back to the OpenCV decoder when it is unavailable.
pydantic==2.9.2
pydantic-settings==2.10.1
redis==5.2.1
pika==1.3.2
opencv-python-headless==4.10.0.84
numpy==1.26.4
prometheus-client==0.21.1
PyYAML==6.0.2
SQLAlchemy==2.0.43
psycopg2-binary==2.9.10
