# VITE_API_URL=| 'http://localhost:8000/api'
VITE_API_URL=http://localhost:8000/api/v1
VITE_MEDIA_BASE_URL=http://localhost:8080

# Real-time alerts WebSocket. Leave unset to auto-derive from VITE_API_URL
# (http→ws). Set explicitly if the WS is served from a different host.
# VITE_WS_URL=ws://localhost:8000/api/v1/ws/alerts

# Auth0 — matches the backend's Auth0 config (src/core/config.py)
VITE_AUTH0_DOMAIN=dev-d2x11lcqgic0sb80.us.auth0.com
VITE_AUTH0_CLIENT_ID=mdYChGQzvXBmeDLpcCCfcuOG0yPhLe87
# Auth0 API Identifier (audience) — must match the backend's auth0_audience
VITE_AUTH0_AUDIENCE=https://master-solution-api
